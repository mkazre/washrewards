// App configuration. The mobile app talks to the live Laravel API on AWS.
// Admin panel + API: https://api.washrewards.online
export const API_BASE_URL = 'https://api.washrewards.online/api';

// Secure-store key for the Sanctum bearer token.
export const TOKEN_KEY = 'wr_auth_token';
